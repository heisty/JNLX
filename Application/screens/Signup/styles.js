import {
	StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
	header: {
		
		marginTop: 10,
		color: '#FFFFFF',
		fontSize: 20,
		fontWeight: 'bold',
		textAlign: 'center'
	},
	btnText: {
		color: '#FFFFFF',
		fontSize: 12,
		textAlign: 'center',
	}
});

module.exports = styles;